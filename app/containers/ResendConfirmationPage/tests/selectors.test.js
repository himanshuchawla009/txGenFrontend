// import { fromJS } from 'immutable';
// import { selectResendConfirmationPageDomain } from '../selectors';

describe('selectResendConfirmationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
